[pytest]
; addopts = -p no:warnings
markers = 
  slow: marks tests as slow (deselect with '-m "not slow"')
filterwarnings =
  ; Ignore Music21!
  ; ignore:.*is not defined on StreamIterators*:PendingDeprecationWarning:
